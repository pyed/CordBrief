# Binaries and compiled products
bin/
dist/
build/
*.exe
*.exe~
*.dll
*.so
*.dylib
/cordbrief

# Go test and coverage artifacts
*.out
*.test

# Node / JS dependencies and build products
node_modules/

# Local configs and secrets (must never be committed)
config.json
.env
.env.*
!.env.example

# State, journals, and runtime data
cordbrief-state*.json
*.ndjson
*.log
*.tmp
*.temp

# Discord and Desktop runtime state (if mounted or dropped locally)
.config/
keyrings/
Crashpad/
Local Storage/
Session Storage/
Cache/
GPUCache/
Cookies*

# Keep local runtime secrets and snapshots out of source control.
credentials.json
oauth-token*.json
secrets.json
core-ack.json
recovery-state.json
collector-status.json
collector-runtime-status.json
retention-manifest.json
*.ids.json
/data/
/exchange/
/backups/
*.tar
*.tar.gz

# Editor and IDE caches
.idea/
.vscode/
*.swp
*.swo
*~

# Operating system artifacts
.DS_Store
Thumbs.db
ehthumbs.db
[Dd]esktop.ini

# Agent / unlazy local caches and locks
.agents/
.unlazy/
.unlazy-hook-state.json
.claude/
